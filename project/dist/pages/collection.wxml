
  <scroll-view scroll-y class="img">
    <view class="row dis-six ">
      <view  wx:for="{{src}}" wx:key="index">
        <view class="boxes">
          <image class="height" src="{{item}}" bindtap="onTapImg"/>
        </view>
      </view>
      <view bindtap="tap">
        <view class="boxes">
          <image class="height" src="{{c3}}"/>
        </view>
      </view>
    </view>

    <view  wx:if="{{show}}" class="inputContent">
      <view class="center">
        <input/>
      </view>
      <button class="btn-area" bindtap="bindButtonTap">确认</button>
    </view>

  </scroll-view>