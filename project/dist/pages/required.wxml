
  <view class="img">
    <view class="section">
      <view class="btn-area">
        <view class="center" bindtap="bindButtonTap">搜书名</view>
      </view>
      <input class="holder" placeholder="{{placeholder}}" placeholder-style="color:white" focus="{{focus}}"/>
    </view>
  </view>