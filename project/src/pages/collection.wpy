<style lang="less">
.globalmargin {
  background-size: 100% 100%; 
  // position: fixed;
  margin-left: 5%;
  margin-right: 5%;
}
.display {
  display: flex;
  flex-wrap: wrap;
  margin-top: 2%;
}
.row {
  display: flex;
  align-items: center;
  justify-content: flex-start;
}
.column {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}
.spacebetween {
  justify-content: space-between;
}
.input {
  border: 1px solid #e6e6e6;
  background-color: #e6e6e6;
  width: 80%;
  border-radius: 3rem;
  display: flex;
  justify-content: center;
  align-items: center;
  position: fixed;
  background-attachment: fixed;
  z-index: 100;
}
.QRcode {
  height: 1.5rem;
  width: 1.5rem;
  margin-left: 5%;
}
.margin {
  margin-top: 3%;
  margin-bottom: 3%;
}
.size {
  height: 5rem;
  width: 47.5%;
  border-radius: 0.3rem;
  background-color: gray;
}
.line {
  height: 1px;
  width: 38vw;
  background-color: #cdcdcd;
}
.classify {
  font-size: 0.7rem;
  color: #cdcdcd;
  margin-left: 4px;
  margin-right: 4px;
}
.flex {
  flex: 1;
}
.imgsize {
  width: 30vw;
  background-color: rgba(36, 36, 36, 0.1);
  border-radius: 0.3rem;
  margin: 5%;
}
</style>

<template>
  <view class="globalmargin">
    <view class="row">
      <navigator class="input" url="./search" open-type="navigate">
        <view style="color: gray">🔍 搜索</view>
      </navigator>
      <view style="display: flex; justify-content: flex-end; width: 100%;" @tap="scancode">
        <image  class="QRcode" src="{{QRcode}}"></image>
      </view>
    </view>

    <view class="row spacebetween margin">
      <view  class="size column" style="position:relative;">
        <view class="column" style="position: absolute;">
          <text style="color: white; letter-space: 15px;">与动物</text>
          <text style="color: white; font-size: .6rem;">with animal</text>
        </view>
        <image style="width:100%; height:100%;" src="{{left}}"></image>
      </view> 
    <view  class="size column" style="position:relative;">
        <view class="column" style="position: absolute;">
          <text style="color: white; letter-space: 15px;">与自然</text>
          <text style="color: white; font-size: .6rem;">with environment</text>
        </view>
        <image style="width:100%; height:100%;" src="{{right}}"></image>
      </view>
    </view>

    <view class="row">
      <view class="line"></view>
      <text class="classify" style="word">分类</text>
      <view class="line"></view>
    </view>

    <view class="display">
      <view wx:for="{{box}}" wx:key="index" id="{{index}}" class="row spacebetween" style="position:relative;">
        <text style="color: white; position: absolute; top: 0; margin: 5% 0 0 10%;">{{item.title}}</text>
        <image  mode="widthFix" class="imgsize" src="{{item.icon}}"></image>
      </view>
    </view>

  </view>
</template>

<script>
import wepy from 'wepy'
import content from '../util/content'
export default class Index extends wepy.page {
  config = {
    navigationBarTitleText: ''
  }

  data = {
    box: [],
    left: '../images/Weanimal.jpeg',
    right: '../images/Weworld.jpeg',
    QRcode: '../images/scanCode.png'
  }

  methods = {
    scancode() {
      wepy.scanCode({
        success: (res) => {
          console.log(res)
        }
      })
    }
  }

  onLoad() {
    let self = this
    self.box = content
  }
  onPullDownRefresh() {
    setTimeout(function() {
      wepy.stopPullDownRefresh()
    }, 1000)
  }
  onShareAppMessage(res) {
    if (res.from === 'button') {
      // 来自页面内转发按钮
      console.log(res.target)
    }
    return {
      title: '自定义转发标题',
      path: '/page/user?id=123',
      success: function(res) {
        // 转发成功
      },
      fail: function(res) {
        // 转发失败
      }
    }
  }
}
</script>
